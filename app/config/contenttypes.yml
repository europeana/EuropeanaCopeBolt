## Defaults nodes. Does not create a Contenttype
__nodes:
  record_defaults: &record_defaults
    title:
      type: text
      label: "Title (required)"
      variant: inline
      required: true
      postfix: "The main title is shown on individual pages as the page title and on listings. Also used for related content teasers"
      group: Main
    intro:
      type: html
      label: "Intro"
      height: 150px
      postfix: "Shown on individual pages before the main body text"
      group: Main
    body:
      type: html
      label: "Body"
      height: 300px
      postfix: "Shown on individual pages as main body text"
      group: Main
  teaser_defaults: &teaser_defaults
    teaser:
      type: html
      label: "Teaser"
      height: 150px
      postfix: "The teaser is shown on listing pages instead of the intro and body fields. Also used for related content teasers"
      group: Teaser
    teaser_image:
      type: image
      label: "Teaser image"
      postfix: "The teaser image is shown on listing pages instead of the main image. Also used for related content teasers"
  cta_defaults: &cta_defaults
    button_text:
      type: text
      label: "Call to action button text"
      group: "CTA"
    button_link:
      type: text
      label: "Call to action button link"
      group: "CTA"
    button_intro:
      type: text
      label: "Optional prefix text for the call to action button"
      group: "CTA"
  media_defaults: &media_defaults
    image:
      type: image
      label: "Main image"
      attrib: title
      postfix: "The main image is displayed on individual pages."
      required: true
      extensions: [ gif, jpg, png ]
      group: "Media"
    attachments:
      type: filelist
      required: false
      label: "Attachments (optional)"
      group: "Media"
    filelist_downloads:
      type: text
      class: narrow
      label: "Document folder"
      postfix: "Add the name of the folder to show as a 'File browser' listing, relative to the base 'files/' folder. If left blank, no file-browser will be shown."
      group: "Media"
  structure_defaults: &structure_defaults
    subsite:
      type: select
      label: "Subsite"
      values:
        unknown: Unknown
        pro: Europeana Pro
        labs: Europeana Labs
        research: Europeana Research
        zoho: ZOHO CRM
        publications: Publications
        pressreleases: Pressreleases
        him: Heritage in motion
      group: meta
      postfix: "The site this content was imported from."
      prefix: "<div><h3>Site structure data</h3></div>"
    structure_parent:
      type: select
      default: ''
      values: structures/id,title
      autocomplete: true
      label: "Select structure tree parent"
      group: meta
    structure_sortorder:
      type: integer
      label: "Sort order in structure tree parent"
      group: meta
    slug:
      type: slug
      uses: title
      group: meta
    hide_list:
      type: checkbox
      label: "Hide from list views"
      postfix: "Check to hide this page from listings."
      group: meta
    hide_related:
      type: checkbox
      label: "Hide from related content"
      postfix: "Check to hide this page from related content sections."
      group: meta
    support_navigation:
      type: checkbox
      label: "Support navigation"
      postfix: "Check to automatically generate support navigation from the headings in the main body text."
      group: meta
    template: &template_defaults
      type: templateselect
      group: meta
  support_defaults: &support_defaults
    contact_record:
      type: text
      label: "Contact record"
      class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-1'
      prefix: '<div><h3>Support Contact</h3><p>The support contact is usually a europeana staff member who is designated as the primary contact about the current record.</p></div>'
      postfix: '<p>Use this to select a contact to display along this record. If nothing is selected the contact for the parent structure will be selected</p>'
      group: Support contact
    contact_blurb:
      type: html
      required: false
      label: "Contact blurb"
      height: 150px
      postfix: '<p>Use this field to override the introduction from the contact record with a custom blurb.</p>'
      group: Support contact
  contact_defaults: &contact_defaults
    contact_name:
      type: text
      label: "Name"
      required: false
      variant: inline
      class: narrow
      prefix: '<div><h3>External Contact</h3><p>The external contact is usually someone in the network who is designated as the primary contact about the current record.</p></div>'
    contact_email:
      type: text
      label: "Email"
      required: false
      variant: inline
      class: narrow
    contact_website:
      type: text
      label: "Website"
      required: false
      variant: inline
      class: narrow
  network_default_values: &network_default_values
    values:
      "(not set)": "(not set)"
      "Founding Member": "Founding Member"
      "Governing Board": "Governing Board"
      "Management Board": "Management Board"
      "Members Council": "Members Council"
      "Network Association Member": "Network Association Member"
      "MC Election Candidate": "MC Election Candidate"
      "Partner": "Partner"
      "Not a Network member": "Not a Network member"
      "Network Association Member": "Network Association Member"
      "Members Council Member": "Members Council Member"
      "Network Association Management Board Member": "Network Association Management Board Member"
      "Member States Representative": "Member States Representative"
      "Representative of Content Holder Associations": "Representative of Content Holder Associations"
      "Representative of Specific Fields of Expertise": "Representative of Specific Fields of Expertise"
      "MC Election Candidate": "MC Election Candidate"
      "Observer": "Observer"
      "Friend of Europeana": "Friend of Europeana"
  relation_pages: &relation_pages
    pages:
      label: "Pages"
      multiple: true
      order: -id
      format: "{{ item.title|escape }}"
      prefix: "<div><h3>Usage of relations</h3><p>Use these relations only for hard linking of items. For grouping items together the Tags are better suited.</p><br></div>"
  relation_posts: &relation_posts
    posts:
      label: "Posts"
      multiple: true
      order: -datepublish
      format: "{{ item.title|escape }}"
  relation_data: &relation_data
    data:
      label: "Data"
      multiple: true
      order: title
      format: "{{ item.title|escape }}"
  relation_projects: &relation_projects
    projects:
      label: "Projects"
      multiple: true
      order: title
      format: "{{ item.title|escape }}"
  relation_events: &relation_events
    events:
      label: "Events"
      multiple: true
      order: -datepublish
      format: "{{ item.title|escape }}"
  relation_persons: &relation_persons
    persons:
      label: "Contacts"
      multiple: true
      order: last_name
      format: "{{ item.last_name }}, {{ item.first_name }}"
  relation_resources: &relation_resources
    resources:
      label: "Footer resources"
      multiple: true
      order: weight
      format: "{{ item.title|escape }}"
      postfix: "Choose one or more footer resource blocks. The resources will be used as footers for the whole substructure. Resources wille be ordered by the weight field."
  all_relations: &all_relations
    relations:
      <<: *relation_pages
      <<: *relation_posts
      <<: *relation_data
      <<: *relation_projects
      <<: *relation_events
      <<: *relation_persons


#################################
#
# PAGE
#
#################################
pages:
  name: Pages
  singular_name: Page
  fields:
    <<: *record_defaults
    body:
      type: html
      label: "Body (optional)"
    pagefooter:
      type: html
      label: "Pagefooter (optional)"
      height: 100px
      postfix: "Shown on individual pages below the main body text"
      group: Main
    pagetype:
      type: select
      label: Type of page
      default: page
      required: true
      values:
        page: Normal page
        listing: Listing page
        landingpage-nohighlight: Landing page (without highlights)
        landingpage: Landing page (with highlights)
        splashpage: Splashpage
      group: Modules
    highlighttitle:
      type: text
      label: Title (optional)
      info: The title will be shown as title for the highlights, if the title is empty the block will not have a title either
      postfix: The title will be shown as title for the highlights, if the title is empty the block will not have a title either.
      placeholder: Optional higlights title
      variant: inline
      group: Modules
    highlightsources:
      type: select
      label: Sources
      default: posts
      info: Use this to select what contenttype the highlights will load
      postfix: Use this to select what contenttype the highlights will load, the default is <b>posts</p> which should be the correct choice.
      values:
        pages: Pages, Listings, Landingpages, Structures and Collections
        persons: Persons
        posts: Blogposts, Pressreleases, Publications and News
        data: Data, Applications, Documentation
        projects: Projects, Taskforces and Workinggroups
        jobs: Jobs
        events: Events
        resources: Resource blocks
      group: Modules
    highlightsubtype:
      type: select
      label: Selected type
      variant: inline
      placeholder: Enter a subtype here
      info: The selected subtype will be used to filter the records that are shown in the highlightbar
      values:
        posttype: '-** Post type **-'
        Blog: "Blog"
        Pressrelease: "Pressrelease"
        Publication: "Publication"
        News: "News"
        network_participation: '-** Network Participation **-'
        "Founding Member": "Founding Member"
        "Governing Board": "Governing Board"
        "Management Board": "Management Board"
        "Members Council": "Members Council"
        "Network Association Member": "Network Association Member"
        "MC Election Candidate": "MC Election Candidate"
        "Partner": "Partner"
        "Not a Network member": "Not a Network member"
        "Network Association Member": "Network Association Member"
        "Members Council Member": "Members Council Member"
        "Network Association Management Board Member": "Network Association Management Board Member"
        "Member States Representative": "Member States Representative"
        "Representative of Content Holder Associations": "Representative of Content Holder Associations"
        "Representative of Specific Fields of Expertise": "Representative of Specific Fields of Expertise"
        "MC Election Candidate": "MC Election Candidate"
        "Observer": "Observer"
        "Friend of Europeana": "Friend of Europeana"
        projecttype: '-** Project type **-'
        "TaskForce": "TaskForce"
        "WorkingGroup": "WorkingGroup"
        "Project": "Project"
      class: preload-categoryslug
      group: Modules
    highlightcategory:
      type: text
      label: Selected category
      variant: inline
      placeholder: Enter a category slug here
      info: The selected category will be used to filter the records that are shown in the highlightbar
      class: preload-categoryslug
      group: Modules
    highlightlink:
      type: text
      label: Link (optional)
      postfix: If present, this link will be used as the url for the <b>more</b> link. If not present no extra link will be displayed. The link should start with a slash "<b>/</b>" or with 'https://'
      placeholder: Optional higlights link
      variant: inline
      group: Modules
    modules:
      label: Modules
      type: repeater
      separator: true
      prefix: Modules are blocks of content from the other parts of the site. Use the modules to build elaborate landingpages.
      group: Modules
      fields:
        enabled:
          type: select
          label: Show or Hide
          default: disabled
          required: true
          values:
            enabled: Show this module
            disabled: Do not show this module
        title:
          type: text
          label: Title (optional)
          info: The title will be shown as title for the block, if the title is empty the block will not have a title either
          placeholder: Optional block title
          variant: inline
          class: 'titletoggle'
        subtitle:
          type: text
          label: Subtitle (optional)
          info: The subtitle is may be one short line of text
          placeholder: Optional short subtitle
          variant: inline
        anchortitle:
          type: text
          label: Anchor Title (optional)
          info: If you enter an anchor title, this module will get an in-page link with that title to jump to this module.
          placeholder: Optional anchor title
          variant: inline
        templates:
          type: select
          default: teasers
          info: Use the template to select how the module will look
          postfix: 'Use the template to select how the module will look <a class="cheatsheet btn btn-info btn-xs pull-right"><i class="fa fa-comment-o" aria-hidden="true"></i> view the template cheatsheet</a>'
          values:
            banner:            "One wide banner view (_banner.twig)"
            #bannerdouble:      "Two banners view (_bannerdouble.twig)"
            bannertriple:      "Three banners view (_bannertriple.twig)"
            title:             "Title with a link (_title.twig)"
            teaser:            "Teaser view (_teaser.twig)"
            listing:           "Listing of records (_listing.twig)"
            #listingexpander:   "Expander block with records - vertical accordeon (_listingexpander.twig)"
            listingnews:       "Two news postings and a link to more (_listingnews.twig)"
            listingtable:      "Person listing as a table (_listingtable.twig)" #Tableversie
            listingsemistatic: "Three static intro items (_listingsemistatic.twig)"
            listingthumbnails: "Four columns with round thumbnailed links (_listingthumbnails.twig)"
            detail:            "Full details for single items (_detail.twig)"
            breaker:           "One Section breaker, call to action (_breaker.twig)"
            breakerdouble:     "Two Section breakers, call to action (_breakerdouble.twig)"
            streamercolumn:    "Column streamers (_streamercolumn.twig)"
            calltoactionform:  "Call to action form main page (_calltoactionform.twig)"
            calltoactionlink:  "Call to action button main page (_calltoactionlinks.twig)"
            calltoactionblog:  "Call to action blog page (_calltoactionblog.twig)"
            #filecontent:       "File content block (_filecontent.twig)"
            fileslisting:      "File list block (_fileslisting.twig)"
            body:              "Module body field - for extra body blocks between modules (_body.twig)"
            collapsedcontent:  "Collapsed content clicks open with click on header (_collapsedcontent.twig)"
        body:
          type: html
          label: "Module body (optional)"
          height: 100px
          postfix: "Shown in module blocks as body text"
          class: 'showfortemplate-body'
        icon:
          type: image
          label: Streamer Icon (optional)
          info: Add image.
          placeholder: Optional icon
          variant: inline
          class: 'showfortemplate-listingstreamers'
        ordering:
          type: select
          label: Order- and filtering
          default: latest
          info: Use the ordering to select how the module will load records
          postfix: Use the ordering and filtering to select how the module will load records
          values:
            specified: Select some records below
            latest: The newest records first (with pagination)
            latest_unpaged: The newest records first (without pagination)
            tag: Tag (with pagination)
            tag_unpaged: Tag (without pagination)
            category: Category (with pagination)
            category_unpaged: Category (without pagination)
            oldest: Chronological with the oldest records first (with pagination)
            oldest_unpaged: Chronological with the oldest records first (without pagination)
            calendar: Calendar (based on start and end date)
            calendar_upcoming: Calendar upcoming (based on start and end date)
            calendar_archive: Calendar archive (based on start and end date)
            alphabetical: Alphabetically ordered by title
            random: A number of random records
        sources:
          type: select
          default: posts
          info: Use this to select what contenttype the module will load
          postfix: Use this to select what contenttype the module will load
          values:
            pages: Pages, Listings, Landingpages, Structures and Collections
            persons: Persons
            posts: Blogposts, Pressreleases, Publications and News
            data: Data, Applications, Documentation
            projects: Projects, Taskforces and Workinggroups
            jobs: Jobs
            events: Events
            resources: Banners, Calls to action and Blocks
        override_amount:
          type: integer
          label: Amount
          default: 3
          variant: inline
          info: "If this field is set this number will be used as the number of items in the listing"
          prefix: '<hr>'
        basetag:
          type: text
          label: Selected tag
          variant: inline
          placeholder: Enter a tag here
          info: The selected tag will be used to filter the records that are shown
        basecategory:
          type: text
          label: Selected category
          variant: inline
          placeholder: Enter a category slug here
          class: preload-categoryslug
          info: The selected category will be used to filter the records that are shown
        selected_pages:
          type: text
          label: "Select Pages"
          class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
        filter_persons:
          type: select
          required: false
          multiple: false
          expanded: true
          label: Filter persons
          postfix: You can optionally choose one of the network participation levels to filter
          <<: *network_default_values
        selected_persons:
          type: text
          label: "Select Persons"
          class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
        filter_posts:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter post type"
          values:
            "Blog": "Blog"
            "Pressrelease": "Pressrelease"
            "Publication": "Publication"
            "News": "News"
        selected_posts:
          type: text
          label: "Select Posts"
          class: 'ajaxselector ct-posts fields-id--title--status key-id results-auto'
        filter_data:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter data type"
          values:
            "Data": "Data"
            "Tool": "Tool"
            "Showcase": "Showcase"
            "Documentation": "Documentation"
            "API": "API"
        selected_data:
          type: text
          label: "Select data"
          class: 'ajaxselector ct-data fields-id--title--status key-id results-auto'
        filter_projects:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter project type"
          values:
            "TaskForce": "TaskForce"
            "WorkingGroup": "WorkingGroup"
            "Project": "Project"
        selected_projects:
          type: text
          label: "Select Projects"
          class: 'ajaxselector ct-projects fields-id--title--status key-id results-auto'
        selected_jobs:
          type: text
          label: "Select Jobs"
          class: 'ajaxselector ct-jobs fields-id--title--status key-id results-auto'
        selected_events:
          type: text
          label: "Select Events"
          class: 'ajaxselector ct-events fields-id--title--status key-id results-auto'
        selected_resources:
          type: text
          label: "Select Banners, Calls to action or Blocks"
          class: 'ajaxselector ct-resources fields-id--title--status key-id results-auto'
        override_css_id:
          type: text
          label: DOM ID
          variant: inline
          placeholder: custom css ID
          info: If you need to add a custom css ID you can enter that here
        override_css_class:
          type: text
          label: CSS Class
          variant: inline
          placeholder: custom css class
          info: If you need to add custom css classes you can enter them here
    <<: *teaser_defaults
    <<: *cta_defaults
    <<: *media_defaults
    imagegallery:
      type: imagelist
      label: "Images gallery (optional)"
      postfix: "Add images to generate a picture gallery. If left blank, no gallery will be shown."
    <<: *structure_defaults
    <<: *support_defaults
    <<: *contact_defaults
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  recordsperpage: 50
  icon_many: "fa:files-o"
  icon_one: "fa:file"

#################################
#
# BLOG POST
#
#################################
posts:
  name: 'Posts'
  singular_name: 'Post'
  display_name: 'Posts'
  slug: 'posts'
  singular_slug: 'post'
  fields:
    title:
      type: text
      label: "Title (required)"
      variant: inline
      required: true
      postfix: "The main title is shown on individual pages as the page title and on listings. Also used for related content teasers"
      group: Main
    posttype:
      type: select
      required: true
      multiple: false
      expanded: true
      default: "Blog"
      label: "Post type"
      values:
        Blog: "Blog"
        Pressrelease: "Pressrelease"
        Publication: "Publication"
        News: "News"
      postfix: "Select if this record is a Blog, Pressrelease, Publication or other News record"
    intro:
      type: html
      label: "Intro"
      height: 150px
      postfix: "Shown on individual pages before the main body text"
      group: Main
    modules:
      label: Modules
      type: repeater
      separator: true
      prefix: Modules are blocks of content from the other parts of the site.
      group: Modules
      fields:
        enabled:
          type: select
          label: Show or Hide
          default: disabled
          required: true
          values:
            enabled: Show this module
            disabled: Do not show this module
        title:
          type: text
          label: Title (optional)
          info: The title will be shown as title for the block, if the title is empty the block will not have a title either
          placeholder: Optional block title
          variant: inline
          class: 'titletoggle'
        subtitle:
          type: text
          label: Subtitle (optional)
          info: The subtitle is may be one short line of text
          placeholder: Optional short subtitle
          variant: inline
        anchortitle:
          type: text
          label: Anchor Title (optional)
          info: If you enter an anchor title, this module will get an in-page link with that title to jump to this module.
          placeholder: Optional anchor title
          variant: inline
        templates:
          type: select
          default: teasers
          info: Use the template to select how the module will look
          postfix: 'Use the template to select how the module will look <a class="cheatsheet btn btn-info btn-xs pull-right"><i class="fa fa-comment-o" aria-hidden="true"></i> view the template cheatsheet</a>'
          values:
            body:              "Module body field - for extra body blocks between modules (_body.twig)"
            collapsedcontent:  "Collapsed content clicks open with click on header (_collapsedcontent.twig)"
            image:             "One wide image view (_image.twig)"
            banner:            "One wide banner view (_banner.twig)"
            #bannerdouble:      "Two banners view (_bannerdouble.twig)"
            bannertriple:      "Three banners view (_bannertriple.twig)"
            listingtable:      "Person listing as a table (_listingtable.twig)" #Tableversie
            listingthumbnails: "Four columns with round thumbnailed links (_listingthumbnails.twig)"
            calltoactionblog:  "Call to action blog page (_calltoactionblog.twig)"
            #filecontent:       "File content block (_filecontent.twig)"
            fileslisting:      "File list block (_fileslisting.twig)"
        body:
          type: html
          label: "Module body (optional)"
          height: 100px
          postfix: "Shown in module blocks as body text"
          class: 'showfortemplate-body'
        image:
          type: image
          label: Image (optional)
          attrib: title
          info: Add image.
          variant: inline
          class: 'showfortemplate-image'
        ordering:
          type: select
          label: Order- and filtering
          default: latest
          info: Use the ordering to select how the module will load records
          postfix: Use the ordering and filtering to select how the module will load records
          values:
            specified: Select some records below
            latest: The newest records first (with pagination)
            latest_unpaged: The newest records first (without pagination)
            tag: Tag (with pagination)
            tag_unpaged: Tag (without pagination)
            category: Category (with pagination)
            category_unpaged: Category (without pagination)
            oldest: Chronological with the oldest records first (with pagination)
            oldest_unpaged: Chronological with the oldest records first (without pagination)
            calendar: Calendar (based on start and end date)
            calendar_upcoming: Calendar upcoming (based on start and end date)
            calendar_archive: Calendar archive (based on start and end date)
            alphabetical: Alphabetically ordered by title
            random: A number of random records
        sources:
          type: select
          default: posts
          info: Use this to select what contenttype the module will load
          postfix: Use this to select what contenttype the module will load
          values:
            pages: Pages, Listings, Landingpages, Structures and Collections
            persons: Persons
            posts: Blogposts, Pressreleases, Publications and News
            data: Data, Applications, Documentation
            projects: Projects, Taskforces and Workinggroups
            jobs: Jobs
            events: Events
            resources: Banners, Calls to action and Blocks
        override_amount:
          type: integer
          label: Amount
          default: 3
          variant: inline
          info: "If this field is set this number will be used as the number of items in the listing"
          prefix: '<hr>'
        basetag:
          type: text
          label: Selected tag
          variant: inline
          placeholder: Enter a tag here
          info: The selected tag will be used to filter the records that are shown
        basecategory:
          type: text
          label: Selected category
          variant: inline
          placeholder: Enter a category slug here
          class: preload-categoryslug
          info: The selected category will be used to filter the records that are shown
        selected_pages:
          type: text
          label: "Select Pages"
          class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
        filter_persons:
          type: select
          required: false
          multiple: false
          expanded: true
          label: Filter persons
          postfix: You can optionally choose one of the network participation levels to filter
          <<: *network_default_values
        selected_persons:
          type: text
          label: "Select Persons"
          class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
        filter_posts:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter post type"
          values:
            "Blog": "Blog"
            "Pressrelease": "Pressrelease"
            "Publication": "Publication"
            "News": "News"
        selected_posts:
          type: text
          label: "Select Posts"
          class: 'ajaxselector ct-posts fields-id--title--status key-id results-auto'
        filter_data:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter data type"
          values:
            "Data": "Data"
            "Tool": "Tool"
            "Showcase": "Showcase"
            "Documentation": "Documentation"
            "API": "API"
        selected_data:
          type: text
          label: "Select data"
          class: 'ajaxselector ct-data fields-id--title--status key-id results-auto'
        filter_projects:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter project type"
          values:
            "TaskForce": "TaskForce"
            "WorkingGroup": "WorkingGroup"
            "Project": "Project"
        selected_projects:
          type: text
          label: "Select Projects"
          class: 'ajaxselector ct-projects fields-id--title--status key-id results-auto'
        selected_jobs:
          type: text
          label: "Select Jobs"
          class: 'ajaxselector ct-jobs fields-id--title--status key-id results-auto'
        selected_events:
          type: text
          label: "Select Events"
          class: 'ajaxselector ct-events fields-id--title--status key-id results-auto'
        selected_resources:
          type: text
          label: "Select Banners, Calls to action or Blocks"
          class: 'ajaxselector ct-resources fields-id--title--status key-id results-auto'
        override_css_id:
          type: text
          label: DOM ID
          variant: inline
          placeholder: custom css ID
          info: If you need to add a custom css ID you can enter that here
        override_css_class:
          type: text
          label: CSS Class
          variant: inline
          placeholder: custom css class
          info: If you need to add custom css classes you can enter them here
    body:
      type: html
      label: "Body (required)"
      height: 300px
      postfix: "Shown on individual pages as main body text"
      group: Main
    author:
      label: "Author(s)"
      type: text
      class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
    isbn:
      label: "ISBN"
      type: text
    <<: *teaser_defaults
    <<: *cta_defaults
    <<: *support_defaults
    <<: *contact_defaults
    <<: *media_defaults
    <<: *structure_defaults
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  recordsperpage: 100
  sort: -datepublish
  icon_many: "fa:quote-left"
  icon_one: "fa:quote-right"

#################################
#
# DATA
#
#################################
data:
  name: Data
  singular_name: Data
  fields:
    title:
      type: text
      label: "Title (required)"
      variant: inline
      required: true
      postfix: "The main title is shown on individual pages as the page title and on listings. Also used for related content teasers"
      group: Main
    datatype:
      type: select
      required: true
      multiple: false
      expanded: true
      default: "Data"
      label: "Data type"
      values: [ "Data", "Tool", "Showcase", "Documentation", "API" ]
      postfix: "Select if this record is a data, documentation, tool or API record"
    intro:
      type: html
      label: "Intro"
      height: 150px
      postfix: "Shown on individual pages before the main body text"
      group: Main
    body:
      type: html
      label: "Body (required)"
      height: 300px
      postfix: "Shown on individual pages as main body text"
      group: Main
    modules:
      label: Modules
      type: repeater
      separator: true
      prefix: Modules are blocks of content from the other parts of the site.
      group: Modules
      fields:
        enabled:
          type: select
          label: Show or Hide
          default: disabled
          required: true
          values:
            enabled: Show this module
            disabled: Do not show this module
        title:
          type: text
          label: Title (optional)
          info: The title will be shown as title for the block, if the title is empty the block will not have a title either
          placeholder: Optional block title
          variant: inline
          class: 'titletoggle'
        subtitle:
          type: text
          label: Subtitle (optional)
          info: The subtitle is may be one short line of text
          placeholder: Optional short subtitle
          variant: inline
        anchortitle:
          type: text
          label: Anchor Title (optional)
          info: If you enter an anchor title, this module will get an in-page link with that title to jump to this module.
          placeholder: Optional anchor title
          variant: inline
        templates:
          type: select
          default: teasers
          info: Use the template to select how the module will look
          postfix: 'Use the template to select how the module will look <a class="cheatsheet btn btn-info btn-xs pull-right"><i class="fa fa-comment-o" aria-hidden="true"></i> view the template cheatsheet</a>'
          values:
            body:              "Module body field - for extra body blocks between modules (_body.twig)"
            collapsedcontent:  "Collapsed content clicks open with click on header (_collapsedcontent.twig)"
            image:             "One wide image view (_image.twig)"
            banner:            "One wide banner view (_banner.twig)"
            #bannerdouble:      "Two banners view (_bannerdouble.twig)"
            bannertriple:      "Three banners view (_bannertriple.twig)"
            listingtable:      "Person listing as a table (_listingtable.twig)" #Tableversie
            listingthumbnails: "Four columns with round thumbnailed links (_listingthumbnails.twig)"
            calltoactionblog:  "Call to action blog page (_calltoactionblog.twig)"
            #filecontent:       "File content block (_filecontent.twig)"
            fileslisting:      "File list block (_fileslisting.twig)"
        body:
          type: html
          label: "Module body (optional)"
          height: 100px
          postfix: "Shown in module blocks as body text"
          class: 'showfortemplate-body'
        image:
          type: image
          label: Image (optional)
          attrib: title
          info: Add image.
          variant: inline
          class: 'showfortemplate-image'
        ordering:
          type: select
          label: Order- and filtering
          default: latest
          info: Use the ordering to select how the module will load records
          postfix: Use the ordering and filtering to select how the module will load records
          values:
            specified: Select some records below
            latest: The newest records first (with pagination)
            latest_unpaged: The newest records first (without pagination)
            tag: Tag (with pagination)
            tag_unpaged: Tag (without pagination)
            category: Category (with pagination)
            category_unpaged: Category (without pagination)
            oldest: Chronological with the oldest records first (with pagination)
            oldest_unpaged: Chronological with the oldest records first (without pagination)
            calendar: Calendar (based on start and end date)
            calendar_upcoming: Calendar upcoming (based on start and end date)
            calendar_archive: Calendar archive (based on start and end date)
            alphabetical: Alphabetically ordered by title
            random: A number of random records
        sources:
          type: select
          default: posts
          info: Use this to select what contenttype the module will load
          postfix: Use this to select what contenttype the module will load
          values:
            pages: Pages, Listings, Landingpages, Structures and Collections
            persons: Persons
            posts: Blogposts, Pressreleases, Publications and News
            data: Data, Applications, Documentation
            projects: Projects, Taskforces and Workinggroups
            jobs: Jobs
            events: Events
            resources: Banners, Calls to action and Blocks
        override_amount:
          type: integer
          label: Amount
          default: 3
          variant: inline
          info: "If this field is set this number will be used as the number of items in the listing"
          prefix: '<hr>'
        basetag:
          type: text
          label: Selected tag
          variant: inline
          placeholder: Enter a tag here
          info: The selected tag will be used to filter the records that are shown
        basecategory:
          type: text
          label: Selected category
          variant: inline
          placeholder: Enter a category slug here
          class: preload-categoryslug
          info: The selected category will be used to filter the records that are shown
        selected_pages:
          type: text
          label: "Select Pages"
          class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
        filter_persons:
          type: select
          required: false
          multiple: false
          expanded: true
          label: Filter persons
          postfix: You can optionally choose one of the network participation levels to filter
          <<: *network_default_values
        selected_persons:
          type: text
          label: "Select Persons"
          class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
        filter_posts:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter post type"
          values:
            "Blog": "Blog"
            "Pressrelease": "Pressrelease"
            "Publication": "Publication"
            "News": "News"
        selected_posts:
          type: text
          label: "Select Posts"
          class: 'ajaxselector ct-posts fields-id--title--status key-id results-auto'
        filter_data:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter data type"
          values:
            "Data": "Data"
            "Tool": "Tool"
            "Showcase": "Showcase"
            "Documentation": "Documentation"
            "API": "API"
        selected_data:
          type: text
          label: "Select data"
          class: 'ajaxselector ct-data fields-id--title--status key-id results-auto'
        filter_projects:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter project type"
          values:
            "TaskForce": "TaskForce"
            "WorkingGroup": "WorkingGroup"
            "Project": "Project"
        selected_projects:
          type: text
          label: "Select Projects"
          class: 'ajaxselector ct-projects fields-id--title--status key-id results-auto'
        selected_jobs:
          type: text
          label: "Select Jobs"
          class: 'ajaxselector ct-jobs fields-id--title--status key-id results-auto'
        selected_events:
          type: text
          label: "Select Events"
          class: 'ajaxselector ct-events fields-id--title--status key-id results-auto'
        selected_resources:
          type: text
          label: "Select Banners, Calls to action or Blocks"
          class: 'ajaxselector ct-resources fields-id--title--status key-id results-auto'
        override_css_id:
          type: text
          label: DOM ID
          variant: inline
          placeholder: custom css ID
          info: If you need to add a custom css ID you can enter that here
        override_css_class:
          type: text
          label: CSS Class
          variant: inline
          placeholder: custom css class
          info: If you need to add custom css classes you can enter them here
    <<: *teaser_defaults
    <<: *cta_defaults
    provided_by:
      type: text
      required: false
      label: "Provided by"
      variant: inline
      class: narrow
      group: "Related Links"
    provided_by_link:
      type: text
      required: false
      label: "Provided by link"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    portallink:
      type: text
      required: false
      label: "Portal link"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    apiconsolelink:
      type: text
      required: false
      label: "Api console link"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    link1:
      type: text
      required: false
      label: "External link 1"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    link2:
      type: text
      required: false
      label: "External link 2"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    link3:
      type: text
      required: false
      label: "External link 3"
      variant: inline
      class: narrow
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      group: "Related Links"
    <<: *media_defaults
    <<: *structure_defaults
    <<: *support_defaults
    <<: *contact_defaults
  taxonomy: [ communities, markets, categories, tags, itemtype, languagecoverage, subjects, spatialcoverage, country, timecoverage, educationallevel ]
  <<: *all_relations
  recordsperpage: 100
  icon_many: "fa:object-group"
  icon_one: "fa:object-group"

#################################
#
# PROJECTS
#
#################################
projects:
  name: Projects
  singular_name: Project
  fields:
    title:
      type: text
      label: "Title (required)"
      variant: inline
      required: true
      postfix: "The main title is shown on individual pages as the page title and on listings. Also used for related content teasers"
      group: Main
    subtitle:
      type: text
      label: "Subtitle"
      variant: inline
      postfix: "Shown on Individual pages, Lists, Overviews, Teasers"
    projecttype:
      type: select
      required: true
      multiple: false
      expanded: true
      default: "Project"
      label: "Project type"
      values:
        "TaskForce": "TaskForce"
        "WorkingGroup": "WorkingGroup"
        "Project": "Project"
      postfix: "Select if this record is a project, a workinggroup or a taskforce"
    intro:
      type: html
      label: "Intro"
      height: 150px
      postfix: "Shown on individual pages before the main body text"
      group: Main
    body:
      type: html
      label: "Body (required)"
      height: 300px
      postfix: "Shown on individual pages as main body text"
      group: Main
    department:
      type: select
      label: "Department"
      values: [ "Europeana Network", "Europeana Tech" ]
    url:
      type: text
      required: false
      label: "Link to projects website:"
      variant: inline
      prefix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
    modules:
      label: Modules
      type: repeater
      separator: true
      prefix: Modules are blocks of content from the other parts of the site.
      group: Modules
      fields:
        enabled:
          type: select
          label: Show or Hide
          default: disabled
          required: true
          values:
            enabled: Show this module
            disabled: Do not show this module
        title:
          type: text
          label: Title (optional)
          info: The title will be shown as title for the block, if the title is empty the block will not have a title either
          placeholder: Optional block title
          variant: inline
          class: 'titletoggle'
        subtitle:
          type: text
          label: Subtitle (optional)
          info: The subtitle is may be one short line of text
          placeholder: Optional short subtitle
          variant: inline
        anchortitle:
          type: text
          label: Anchor Title (optional)
          info: If you enter an anchor title, this module will get an in-page link with that title to jump to this module.
          placeholder: Optional anchor title
          variant: inline
        templates:
          type: select
          default: teasers
          info: Use the template to select how the module will look
          postfix: 'Use the template to select how the module will look <a class="cheatsheet btn btn-info btn-xs pull-right"><i class="fa fa-comment-o" aria-hidden="true"></i> view the template cheatsheet</a>'
          values:
            body:              "Module body field - for extra body blocks between modules (_body.twig)"
            collapsedcontent:  "Collapsed content clicks open with click on header (_collapsedcontent.twig)"
            image:             "One wide image view (_image.twig)"
            banner:            "One wide banner view (_banner.twig)"
            #bannerdouble:      "Two banners view (_bannerdouble.twig)"
            bannertriple:      "Three banners view (_bannertriple.twig)"
            listingtable:      "Person listing as a table (_listingtable.twig)" #Tableversie
            listingthumbnails: "Four columns with round thumbnailed links (_listingthumbnails.twig)"
            calltoactionblog:  "Call to action blog page (_calltoactionblog.twig)"
            #filecontent:       "File content block (_filecontent.twig)"
            fileslisting:      "File list block (_fileslisting.twig)"
        body:
          type: html
          label: "Module body (optional)"
          height: 100px
          postfix: "Shown in module blocks as body text"
          class: 'showfortemplate-body'
        image:
          type: image
          label: Image (optional)
          attrib: title
          info: Add image.
          variant: inline
          class: 'showfortemplate-image'
        ordering:
          type: select
          label: Order- and filtering
          default: latest
          info: Use the ordering to select how the module will load records
          postfix: Use the ordering and filtering to select how the module will load records
          values:
            specified: Select some records below
            latest: The newest records first (with pagination)
            latest_unpaged: The newest records first (without pagination)
            tag: Tag (with pagination)
            tag_unpaged: Tag (without pagination)
            category: Category (with pagination)
            category_unpaged: Category (without pagination)
            oldest: Chronological with the oldest records first (with pagination)
            oldest_unpaged: Chronological with the oldest records first (without pagination)
            calendar: Calendar (based on start and end date)
            calendar_upcoming: Calendar upcoming (based on start and end date)
            calendar_archive: Calendar archive (based on start and end date)
            alphabetical: Alphabetically ordered by title
            random: A number of random records
        sources:
          type: select
          default: posts
          info: Use this to select what contenttype the module will load
          postfix: Use this to select what contenttype the module will load
          values:
            pages: Pages, Listings, Landingpages, Structures and Collections
            persons: Persons
            posts: Blogposts, Pressreleases, Publications and News
            data: Data, Applications, Documentation
            projects: Projects, Taskforces and Workinggroups
            jobs: Jobs
            events: Events
            resources: Banners, Calls to action and Blocks
        override_amount:
          type: integer
          label: Amount
          default: 3
          variant: inline
          info: "If this field is set this number will be used as the number of items in the listing"
          prefix: '<hr>'
        basetag:
          type: text
          label: Selected tag
          variant: inline
          placeholder: Enter a tag here
          info: The selected tag will be used to filter the records that are shown
        basecategory:
          type: text
          label: Selected category
          variant: inline
          placeholder: Enter a category slug here
          class: preload-categoryslug
          info: The selected category will be used to filter the records that are shown
        selected_pages:
          type: text
          label: "Select Pages"
          class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
        filter_persons:
          type: select
          required: false
          multiple: false
          expanded: true
          label: Filter persons
          postfix: You can optionally choose one of the network participation levels to filter
          <<: *network_default_values
        selected_persons:
          type: text
          label: "Select Persons"
          class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
        filter_posts:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter post type"
          values:
            "Blog": "Blog"
            "Pressrelease": "Pressrelease"
            "Publication": "Publication"
            "News": "News"
        selected_posts:
          type: text
          label: "Select Posts"
          class: 'ajaxselector ct-posts fields-id--title--status key-id results-auto'
        filter_data:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter data type"
          values:
            "Data": "Data"
            "Tool": "Tool"
            "Showcase": "Showcase"
            "Documentation": "Documentation"
            "API": "API"
        selected_data:
          type: text
          label: "Select data"
          class: 'ajaxselector ct-data fields-id--title--status key-id results-auto'
        filter_projects:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter project type"
          values:
            "TaskForce": "TaskForce"
            "WorkingGroup": "WorkingGroup"
            "Project": "Project"
        selected_projects:
          type: text
          label: "Select Projects"
          class: 'ajaxselector ct-projects fields-id--title--status key-id results-auto'
        selected_jobs:
          type: text
          label: "Select Jobs"
          class: 'ajaxselector ct-jobs fields-id--title--status key-id results-auto'
        selected_events:
          type: text
          label: "Select Events"
          class: 'ajaxselector ct-events fields-id--title--status key-id results-auto'
        selected_resources:
          type: text
          label: "Select Banners, Calls to action or Blocks"
          class: 'ajaxselector ct-resources fields-id--title--status key-id results-auto'
        override_css_id:
          type: text
          label: DOM ID
          variant: inline
          placeholder: custom css ID
          info: If you need to add a custom css ID you can enter that here
        override_css_class:
          type: text
          label: CSS Class
          variant: inline
          placeholder: custom css class
          info: If you need to add custom css classes you can enter them here
    <<: *teaser_defaults
    <<: *cta_defaults
    start_event:
      type: datetime
      required: false
      label: "Project start"
      group: Project Schedule
    end_event:
      type: datetime
      required: false
      label: "Project end"
    <<: *media_defaults
    <<: *structure_defaults
    <<: *support_defaults
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  listing_records: 5
  recordsperpage: 20
  sort: -datepublish
  icon_many: "fa:clipboard"
  icon_one: "fa:clipboard"

#################################
#
# EVENTS
#
#################################
events:
  name: Events
  singular_name: Event
  fields:
    <<: *record_defaults
    location_name:
      type: text
      required: true
      label: "Location Name"
    geolocation:
      type: geolocation
      required: true
      label: "Location Place"
    featured:
      type: checkbox
      label: "Make this event sticky"
      postfix: "<small>One event only. Setting another event to sticky will uncheck this one.</small>"
    modules:
      label: Modules
      type: repeater
      separator: true
      prefix: Modules are blocks of content from the other parts of the site.
      group: Modules
      fields:
        enabled:
          type: select
          label: Show or Hide
          default: disabled
          required: true
          values:
            enabled: Show this module
            disabled: Do not show this module
        title:
          type: text
          label: Title (optional)
          info: The title will be shown as title for the block, if the title is empty the block will not have a title either
          placeholder: Optional block title
          variant: inline
          class: 'titletoggle'
        subtitle:
          type: text
          label: Subtitle (optional)
          info: The subtitle is may be one short line of text
          placeholder: Optional short subtitle
          variant: inline
        anchortitle:
          type: text
          label: Anchor Title (optional)
          info: If you enter an anchor title, this module will get an in-page link with that title to jump to this module.
          placeholder: Optional anchor title
          variant: inline
        templates:
          type: select
          default: teasers
          info: Use the template to select how the module will look
          postfix: 'Use the template to select how the module will look <a class="cheatsheet btn btn-info btn-xs pull-right"><i class="fa fa-comment-o" aria-hidden="true"></i> view the template cheatsheet</a>'
          values:
            body:              "Module body field - for extra body blocks between modules (_body.twig)"
            collapsedcontent:  "Collapsed content clicks open with click on header (_collapsedcontent.twig)"
            image:             "One wide image view (_image.twig)"
            banner:            "One wide banner view (_banner.twig)"
            #bannerdouble:      "Two banners view (_bannerdouble.twig)"
            bannertriple:      "Three banners view (_bannertriple.twig)"
            listingtable:      "Person listing as a table (_listingtable.twig)" #Tableversie
            listingthumbnails: "Four columns with round thumbnailed links (_listingthumbnails.twig)"
            calltoactionblog:  "Call to action blog page (_calltoactionblog.twig)"
            #filecontent:       "File content block (_filecontent.twig)"
            fileslisting:      "File list block (_fileslisting.twig)"
        body:
          type: html
          label: "Module body (optional)"
          height: 100px
          postfix: "Shown in module blocks as body text"
          class: 'showfortemplate-body'
        image:
          type: image
          label: Image (optional)
          attrib: title
          info: Add image.
          variant: inline
          class: 'showfortemplate-image'
        ordering:
          type: select
          label: Order- and filtering
          default: latest
          info: Use the ordering to select how the module will load records
          postfix: Use the ordering and filtering to select how the module will load records
          values:
            specified: Select some records below
            latest: The newest records first (with pagination)
            latest_unpaged: The newest records first (without pagination)
            tag: Tag (with pagination)
            tag_unpaged: Tag (without pagination)
            category: Category (with pagination)
            category_unpaged: Category (without pagination)
            oldest: Chronological with the oldest records first (with pagination)
            oldest_unpaged: Chronological with the oldest records first (without pagination)
            calendar: Calendar (based on start and end date)
            calendar_upcoming: Calendar upcoming (based on start and end date)
            calendar_archive: Calendar archive (based on start and end date)
            alphabetical: Alphabetically ordered by title
            random: A number of random records
        sources:
          type: select
          default: posts
          info: Use this to select what contenttype the module will load
          postfix: Use this to select what contenttype the module will load
          values:
            pages: Pages, Listings, Landingpages, Structures and Collections
            persons: Persons
            posts: Blogposts, Pressreleases, Publications and News
            data: Data, Applications, Documentation
            projects: Projects, Taskforces and Workinggroups
            jobs: Jobs
            events: Events
            resources: Banners, Calls to action and Blocks
        override_amount:
          type: integer
          label: Amount
          default: 3
          variant: inline
          info: "If this field is set this number will be used as the number of items in the listing"
          prefix: '<hr>'
        basetag:
          type: text
          label: Selected tag
          variant: inline
          placeholder: Enter a tag here
          info: The selected tag will be used to filter the records that are shown
        basecategory:
          type: text
          label: Selected category
          variant: inline
          placeholder: Enter a category slug here
          class: preload-categoryslug
          info: The selected category will be used to filter the records that are shown
        selected_pages:
          type: text
          label: "Select Pages"
          class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
        filter_persons:
          type: select
          required: false
          multiple: false
          expanded: true
          label: Filter persons
          postfix: You can optionally choose one of the network participation levels to filter
          <<: *network_default_values
        selected_persons:
          type: text
          label: "Select Persons"
          class: 'ajaxselector ct-persons fields-id--first_name--last_name--status--email key-id results-auto'
        filter_posts:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter post type"
          values:
            "Blog": "Blog"
            "Pressrelease": "Pressrelease"
            "Publication": "Publication"
            "News": "News"
        selected_posts:
          type: text
          label: "Select Posts"
          class: 'ajaxselector ct-posts fields-id--title--status key-id results-auto'
        filter_data:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter data type"
          values:
            "Data": "Data"
            "Tool": "Tool"
            "Showcase": "Showcase"
            "Documentation": "Documentation"
            "API": "API"
        selected_data:
          type: text
          label: "Select data"
          class: 'ajaxselector ct-data fields-id--title--status key-id results-auto'
        filter_projects:
          type: select
          required: false
          multiple: false
          expanded: true
          label: "Filter project type"
          values:
            "TaskForce": "TaskForce"
            "WorkingGroup": "WorkingGroup"
            "Project": "Project"
        selected_projects:
          type: text
          label: "Select Projects"
          class: 'ajaxselector ct-projects fields-id--title--status key-id results-auto'
        selected_jobs:
          type: text
          label: "Select Jobs"
          class: 'ajaxselector ct-jobs fields-id--title--status key-id results-auto'
        selected_events:
          type: text
          label: "Select Events"
          class: 'ajaxselector ct-events fields-id--title--status key-id results-auto'
        selected_resources:
          type: text
          label: "Select Banners, Calls to action or Blocks"
          class: 'ajaxselector ct-resources fields-id--title--status key-id results-auto'
        override_css_id:
          type: text
          label: DOM ID
          variant: inline
          placeholder: custom css ID
          info: If you need to add a custom css ID you can enter that here
        override_css_class:
          type: text
          label: CSS Class
          variant: inline
          placeholder: custom css class
          info: If you need to add custom css classes you can enter them here
    <<: *teaser_defaults
    <<: *cta_defaults
    start_event:
      type: datetime
      required: false
      label: "Event start"
      group: Events Schedule
    unconfirmed_start:
      type: checkbox
      required: false
      label: "Unconfirmed"
    end_event:
      type: datetime
      required: false
      label: "Event end"
    unconfirmed_end:
      type: checkbox
      required: false
      label: "Unconfirmed"
    <<: *media_defaults
    <<: *structure_defaults
    <<: *support_defaults
    <<: *contact_defaults
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  icon_many: "fa:calendar"
  icon_one: "fa:calendar"

#################################
#
# JOBS
#
#################################
jobs:
  name: Jobs
  singular_name: Job
  fields:
    position:
      type: text
      required: true
      label: "Position (required)"
      variant: inline
      class: wide
      group: "Job information"
    body:
      type: html
      label: "Body (required)"
      height: 300px
      postfix: "Shown on individual pages as main body text"
      group: "Job information"
    department:
      type: select
      required: false
      label: "Position will be in this team"
      values:
        - 'Management'
        - 'People & Business Support'
        - 'End User (Collections)'
        - 'Marketing & Communications'
        - 'Re-use'
        - 'Network & Sustainability'
        - 'Programme & Policy'
        - 'Data Partner Services'
        - 'Research & Development'
        - 'Product Owner & Design'
        - 'Software Development'
        - 'Infrastructure'
        - 'The European Library'
      group: "Job information"
    position_type:
      type: select
      required: false
      label: "Contract type"
      values: [ "Full time", "Part time", "Internship", "Full time (Maternity cover)", "Full time (Temp)", "Part time (Temp)" ]
      group: "Job information"
    salary_eur:
      type: text
      required: false
      variant: inline
      label: "Monthly salary (EUR)"
      index: true
      group: "Job information"
    scale_eur:
      type: text
      required: false
      variant: inline
      label: "Scale (EUR)"
      index: true
      group: "Job information"
    deadline:
      type: date
      required: false
      label: "Deadline"
      group: "Job information"
    teampage:
      type: text
      label: "Select Team page (optional)"
      info: Select the page that displays the team (optional)
      class: 'ajaxselector ct-pages fields-id--title--status key-id results-auto'
      data:
        ct: pages
        fields: [id, title, status]
        key: id
    <<: *media_defaults
    <<: *structure_defaults
    slug:
      type: slug
      uses: position
    <<: *support_defaults
    <<: *contact_defaults
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  sort: -datepublish
  icon_many: "fa:money"
  icon_one: "fa:money"

#################################
#
# PERSONS
#
#################################
persons:
  name: Persons
  singular_name: Person
  fields:
    first_name:
      type: text
      label: "First name"
      variant: inline
      class: narrow
      postfix: "<em><small>(required)</small></em>"
      group: "Personal"
    last_name:
      type: text
      label: "Last name"
      postfix: "<em><small>(required)</small></em>"
      variant: inline
      class: narrow
      group: "Personal"
    email:
      type: text
      label: "Email address"
      variant: inline
      pattern: email
      class: narrow
    phone:
      type: text
      label: "Public phone number"
      variant: inline
      class: narrow
      postfix: <small>Format: +31 (0)12 345 6789</small>
    twitter:
      type: text
      label: "Twitter URL"
      variant: inline
      postfix: '<em>For example "https://twitter.com/Europeanaeu" </em>'
      pattern: url
      class: narrow
    linkedin:
      type: text
      required: false
      label: "Linkedin URL"
      variant: inline
      postfix: "<em>URL has to start with http:// or https://</em>"
      pattern: url
      class: narrow
    skype:
      type: text
      required: false
      label: "Skype ID"
      variant: inline
      class: narrow
    image:
      type: image
      label: "Image"
      attrib: title
      extensions: [ gif, jpg, png ]
    teaser:
      type: html
      label: "Teaser"
      height: 150px
      postfix: "The teaser is shown on listing pages instead of the intro and body fields. Also used for related content teasers"
    introduction:
      type: html
      label: "Introduction (Main Bio)"
      height: 200px
      group: "Personal"
    speaker_teaser:
      type: html
      required: false
      label: "Speaker Teaser"
      height: 100px
      group: "Personal"
      prefix: "<div><h3>Speaker introduction</h3><p>Enter a short introduction for use on event pages where this person is a speaker.</p><br></div>"
    speaker_intro:
      type: html
      required: false
      label: "Speaker Introduction"
      height: 200px
      group: "Personal"
    company:
      type: text
      required: false
      label: "Company"
      postfix: (if not Europeana Foundation)
      variant: inline
      class: narrow
      group: "Company"
    job_title:
      type: text
      label: "Job Title"
      variant: inline
      class: narrow
    company_url:
      type: text
      required: false
      label: "Company URL"
      postfix: (if not Europeana Foundation)
      pattern: url
      variant: inline
      class: narrow
    country:
      type: text
      required: false
      class: narrow
      variant: inline
      label: "Country"
    sector:
      type: text
      required: false
      class: narrow
      variant: inline
      label: "Sector"
    team:
      type: select
      required: false
      label: "Team"
      values:
        "(not set)": "(not set)"
        "Management": "Management"
        "People & Business Support": "People & Business Support"
        "Distribution & Engagement": "End User (Collections)"
        "Corporate Communication": "Marketing & Communications"
        "Re-use Services": "Re-use"
        "Network & Sustainability": "Network & Sustainability"
        "Facilitation": "Programme & Policy"
        "Aggregation": "Data Partner Services"
        "Research & Development": "Research & Development"
        "Product Development": "Product Owner & Design"
        "Technology": "Software Development"
        "Infrastructure": "Infrastructure"
        "The European Library": "The European Library"
      group: "Europeana Networks"
    boardposition:
      type: text
      required: false
      class: narrow
      variant: inline
      label: "Europeana Foundation Board position"
#    department:
#      type: select
#      default: 'Our Partner'
#      values:
#        'Europeana Staff': 'Europeana Staff'
#        'Our Partner': 'Our Partner'
    network_participation:
      type: select
      required: false
      multiple: true
      label: "Network participation level"
      <<: *network_default_values
    community:
      type: select
      required: false
      multiple: true
      label: "Interested in"
      values:
        "(not set)": "(not set)"
        "Academic research": "Academic research"
        "Data partner": "Data partner"
        "EuropeanaIPR": "EuropeanaIPR"
        "EuropeanaTech (R&D)": "EuropeanaTech (R&D)"
        "EuropeanaTech": "EuropeanaTech"
        "Cultural creative": "Cultural creative"
        "Innovation": "Innovation"
    community_role:
      type: text
      required: false
      class: narrow
      variant: inline
      label: "Community role"
    europeana_tech:
      type: select
      required: false
      multiple: false
      label: "Member of EuropeanaTech"
      values:
        "(not set)": "(not set)"
        "false": "false"
        "true": "true"
    candidacy_teaser:
      type: html
      required: false
      label: "Candicacy Teaser"
      height: 100px
      group: "Europeana Networks"
      prefix: "<div><h3>MC Election Candidacy</h3><p>Enter a short introduction for use on membership election pages where this person is a elegible.</p><br></div>"
    candidacy_intro:
      type: html
      required: false
      label: "Candicacy Introduction"
      height: 200px
      group: "Europeana Networks"
    uid:
      type: text
      class: narrow
      variant: inline
      group: meta
      prefix: "<div><h3>Zoho data</h3></div>"
    europeana_id:
      type: text
      class: narrow
      variant: inline
#    author_uid:
#      type: text
#      class: narrow
#      variant: inline
#    author_name:
#      type: text
#      class: narrow
#      variant: inline
#    modified_uid:
#      type: text
#      class: narrow
#      variant: inline
#    modified_name:
#      type: text
#      class: narrow
#      variant: inline
#    latime:
#      type: text
#      class: narrow
#      variant: inline
    account_uid:
      type: text
      class: narrow
      variant: inline
    account_name:
      type: text
      class: narrow
      variant: inline
#    statutes_read:
#      type: select
#      required: false
#      multiple: false
#      label: "I have read & agree to the statutes"
#      values:
#        "(not set)": "(not set)"
#        "false": "false"
#        "true": "true"
#    statutes_agree:
#      type: select
#      required: false
#      multiple: false
#      label: "I agree to statutes"
#      values:
#        "(not set)": "(not set)"
#        "false": "false"
#        "true": "true"
    <<: *structure_defaults
    slug:
      type: slug
      uses: [ first_name, last_name ]
    subsite:
      type: select
      label: "Subsite"
      values:
        unknown: Unknown
        pro: Europeana Pro
        labs: Europeana Labs
        research: Europeana Research
        zoho: ZOHO CRM
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  sort: "last_name"
  icon_many: "fa:users"
  icon_one: "fa:user"

#################################
#
# RESOURCES
#
# Resource blocks for frontpage
# and other parts
#
#################################
resources:
  name : Banners, Calls to action, Blocks
  singular_name: Block
  slug: resources
  singular_slug: resource
  fields:
    title:
      type: text
      label: "Title (required)"
      postfix: "Resource blocks will be loaded on pages that they are related to if the template uses them. On landing pages they can also be set as footers for a subtree."
      group: "Content"
    image:
      type: image
      label: "Block image"
      attrib: title
    body:
      type: html
      label: "Block body"
      height: 300px
    htmllink:
      type: text
      label: "Block HTML link (optional)"
      postfix: "<small>E.g: /pages/this-is-my-page . </small>"
      info: If you need your banner to link somewhere, use this field.
    buttontext:
      type: text
      label: "Text on button (optional)"
      info: If your banner has a button, you can add a custom text here.
    weight:
      type: integer
      label: "Weight"
      default: 0
      group: "Attributes"
    cssclass:
      type: text
      label: "CSS Classname (optional)"
    cssid:
      type: text
      label: "CSS ID (optional)"
    ripplelocation:
      label: "Where to place the Europeana ripple? (optional)"
      type: select
      values:
        ripple-topright : "Top right"
        ripple-topleft : "Top left"
        ripple-bottomright : "Bottom right"
        ripple-bottomleft : "Bottom left"
        ripple-no-ripple : "No ripple"
      group: 'Image options'
      prefix: "<small>If used in your template, you can set the style of the europeana ripple here.</small>"
    ripplecolor:
      label: "Ripple color (optional)"
      type: select
      values: [ black, white]
      variant: inline
    rippleweight:
      label: "Transparency of ripple (optional)"
      type: select
      values:
        pct25 : "25 percent transparent"
        pct50 : "50 percent transparent"
        pct75 : "75 percent transparent"
        full : "Not transparent"
      variant: inline
  taxonomy: [ communities, markets, categories, tags ]
  <<: *all_relations
  viewless: true
  sort: weight
  icon_many: "fa:tasks"
  icon_one: "fa:tasks"
  # show_in_menu: System

#################################
#################################
