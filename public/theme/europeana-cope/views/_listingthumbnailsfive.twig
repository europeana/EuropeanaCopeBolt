{% if item is not defined %}
    {% set item = record %}
{% endif %}

<div class="column fifth thumblink-fourcol thumblink-{{ item.contenttype.slug }}">
    {% if item.teaser_image is defined and item.teaser_image is not empty %}
        <img class="round" src="{{ item.teaser_image|thumbnail(250,250,'c')}}" alt="{{ item.title }}"/>
    {% elseif item.values.image is defined and item.values.image is not empty %}
        <img class="round" src="{{ item.values.image|thumbnail(250,250,'c')}}" alt="{{ item.title }}"/>
    {% else %}
        <img class="round" src="{{ 'web_assets/default.png'|thumbnail(250,250,'c')}}" alt="{{ item.title }}"/>
    {% endif %}

    <h3>{{ item.title }}</h3>
    <a href="{{ item.link() }}" class="button-link">
        Learn more
    </a>
</div>
